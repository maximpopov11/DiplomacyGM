# SVG map
# Requirements:
# * Provinces have attribute 'name'
# * Province border coordinates are in the format x,y in the path string
SVG = 'assets/test.svg'

# Layer group in SVG containing province data
PROVINCE_LAYER_ID = 'layer10'
